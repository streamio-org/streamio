# Port to listen on. Localhost-only (127.0.0.1) — never exposed to the network.
PORT2=3001

# Shared secret. Must match LOGIN_CHECK_TOKEN in power-controller's .env.
LOGIN_TOKEN=change-me-to-a-random-value
